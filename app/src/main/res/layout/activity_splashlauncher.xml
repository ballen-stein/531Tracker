<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:background="@drawable/ic_launcher_foreground"
        android:layout_centerInParent="true"/>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:backgroundTint="@color/colorTertiary"
        android:secondaryProgressTint="@color/colorTertiary"
        android:progressTint="@color/colorTertiary"
        android:foregroundTint="@color/colorTertiary"
        android:indeterminateTint="@color/colorTertiary"
        android:rotationY="180"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_marginStart="@dimen/workout_frame_64"
        android:layout_marginEnd="@dimen/workout_frame_64"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>